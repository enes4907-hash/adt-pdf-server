{
  "name": "adt-pdf-server",
  "version": "1.0.0",
  "description": "ADT Treatment Plan PDF generation server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "render-build": "npm install && node node_modules/puppeteer/install.mjs"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^23.11.1",
    "pdf-lib": "^1.17.1"
  },
  "engines": {
    "node": "20.x"
  }
}
